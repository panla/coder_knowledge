# send_task

```text
app.send_task()

name, args=None, kwargs=None,
countdown=None,
    延时任务，多少秒后执行
    默认立即执行
expires=None,
    过期时间，最长等待时间，Datetime or seconds
retries=0,
    重试次数
reply_to=None,
```
